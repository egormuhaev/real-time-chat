import styles from "./NotFoundPage.module.scss";
import cn from "classnames";

const NotFoundPage = () => {
  return <div className={cn(styles.NotFound)}>NotFound</div>;
};

export default NotFoundPage;
